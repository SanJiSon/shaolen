# 🔍 Инструкция по отладке ошибки "Ошибка при загрузке данных"

## Шаг 1: Проверь, что веб-сервер запущен

```bash
python3 webapp_server.py
```

Должно появиться сообщение:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

## Шаг 2: Проверь настройки в .env

Убедись, что в файле `.env` указан правильный URL:

```
BOT_TOKEN=твой_токен
WEBAPP_URL=https://твой-домен.com
```

**Важно:** URL должен быть доступен по HTTPS и указывать на твой веб-сервер.

## Шаг 3: Проверь логи веб-сервера

При открытии WebApp в логах должны появиться сообщения:
- `Запрос миссий для пользователя X`
- `Запрос целей для пользователя X`
- `Запрос привычек для пользователя X`
- `Запрос аналитики для пользователя X`

Если этих сообщений нет - значит запросы не доходят до сервера.

## Шаг 4: Проверь консоль браузера в Telegram

1. Открой WebApp в Telegram
2. Нажми на три точки (меню)
3. Выбери "Открыть в браузере" (если доступно)
4. Или используй DevTools для отладки

В консоли должны быть логи:
- `=== Инициализация пользователя ===`
- `=== Начало загрузки данных ===`
- `📡 Запрос: GET ...`
- `📥 Ответ: 200 OK`

## Шаг 5: Частые проблемы

### Проблема: "Failed to fetch"
**Решение:** Веб-сервер не запущен или недоступен. Проверь:
- Запущен ли `webapp_server.py`
- Правильный ли URL в `WEBAPP_URL`
- Доступен ли сервер по указанному адресу

### Проблема: "404 Not Found"
**Решение:** Неправильный URL или путь. Проверь:
- URL должен быть `https://домен.com` (без `/webapp` или `/index.html`)
- Сервер должен отдавать `index.html` по корневому пути `/`

### Проблема: "500 Internal Server Error"
**Решение:** Ошибка на сервере. Проверь логи веб-сервера для деталей.

### Проблема: "userId не установлен"
**Решение:** WebApp открыт не через Telegram. Убедись, что:
- Открываешь через кнопку в боте
- Не открываешь напрямую в браузере
- Telegram WebApp API доступен

## Шаг 6: Тестирование API напрямую

Проверь доступность API:

```bash
curl http://localhost:8000/api/user/1/missions
```

Должен вернуться JSON (даже пустой массив `[]`).

## Шаг 7: Проверь базу данных

Убедись, что база данных создана:

```bash
ls -la goals_bot.db
```

Если файла нет, запусти веб-сервер - он создаст БД автоматически.

---

**Если проблема не решена:**
1. Скопируй все логи из консоли браузера
2. Скопируй все логи из веб-сервера
3. Проверь, что все зависимости установлены: `pip install -r requirements.txt`
