# 🔧 Настройка с Nginx

Если ты используешь Nginx для отдачи статики, то `webapp_server.py` нужен **только для API endpoints**.

## Архитектура:

```
┌─────────────┐
│   Telegram  │
│    Bot      │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│   Nginx     │─────▶│  Static      │
│  (Port 80)  │      │  Files       │
└──────┬──────┘      │  (HTML/CSS/JS)│
       │             └──────────────┘
       │
       │ API запросы
       ▼
┌─────────────┐
│  FastAPI    │
│  (Port 8000)│
│  API only   │
└─────────────┘
```

## Настройка Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # Статические файлы (HTML, CSS, JS)
    location / {
        root /path/to/telegram_goals_bot/webapp;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # API запросы проксируем на FastAPI
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Статика (CSS, JS) тоже через nginx
    location /static/ {
        root /path/to/telegram_goals_bot/webapp;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## Запуск:

1. **Nginx** - отдает статику (уже настроен)
2. **FastAPI** - только для API:
   ```bash
   python3 webapp_server.py
   ```

## Важно:

- `WEBAPP_URL` в `.env` должен указывать на твой домен (который обслуживает Nginx)
- Nginx проксирует `/api/*` запросы на `localhost:8000` (FastAPI)
- Статика отдается напрямую через Nginx (быстрее)

## Упрощенная версия webapp_server.py (только API):

Если хочешь, можно убрать из `webapp_server.py` всё, что связано со статикой, оставив только API endpoints. Но текущая версия тоже работает - просто статика будет игнорироваться, если её отдает Nginx.
