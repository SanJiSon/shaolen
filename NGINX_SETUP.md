# 🔧 Настройка с Nginx

Если ты используешь Nginx для отдачи статики, то `webapp_server.py` нужен **только для API endpoints**.

## Архитектура:

```
┌─────────────┐
│   Telegram  │
│    Bot      │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│   Nginx     │─────▶│  Static      │
│  (Port 80)  │      │  Files       │
└──────┬──────┘      │  (HTML/CSS/JS)│
       │             └──────────────┘
       │
       │ API запросы
       ▼
┌─────────────┐
│  FastAPI    │
│  (Port 8000)│
│  API only   │
└─────────────┘
```

## Настройка Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # Статические файлы (HTML, CSS, JS)
    location / {
        root /path/to/telegram_goals_bot/webapp;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # API запросы проксируем на FastAPI
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Статика (CSS, JS) тоже через nginx
    location /static/ {
        root /path/to/telegram_goals_bot/webapp;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## Запуск:

1. **Nginx** - отдает статику (уже настроен)
2. **FastAPI** - только для API:
   ```bash
   python3 webapp_server.py
   ```

## Важно:

- `WEBAPP_URL` в `.env` должен указывать на твой домен (который обслуживает Nginx)
- **Обязательно:** Nginx должен проксировать запросы `/api/` на `http://127.0.0.1:8000`. Без этого запросы из WebApp не дойдут до FastAPI, и в логах веб-сервера ничего не появится.
- Статика отдается напрямую через Nginx (быстрее)

## Проверка, что API доступен

**1. С сервера (должен ответить JSON):**
```bash
curl -s http://127.0.0.1:8000/api/health
# Ожидается: {"status":"ok","service":"goals-api"}
```

**2. Через Nginx (с того же сервера или с внешнего адреса):**
```bash
curl -s https://ТВОЙ-ДОМЕН/api/health
# Должен вернуть тот же JSON. Если 404 или HTML — блок location /api/ не настроен или не применяется.
```

**3. При открытии WebApp в Telegram** в логах `python3 webapp_server.py` должны появляться строки:
```
📥 GET /api/health - IP: ...
📥 GET /api/user/123456/missions - IP: ...
Запрос миссий для пользователя 123456
...
```
Если при открытии WebApp в логах **ничего** не появляется — запросы не доходят до FastAPI. Проверь в Nginx конфиге наличие `location /api/ { proxy_pass http://127.0.0.1:8000; ... }` и перезагрузи Nginx: `nginx -t && systemctl reload nginx`.

## Проверка админки и диагностика (почему открывается главная вместо админки)

Если при открытии `https://ваш-домен/admin.html?token=...` или `/admin?token=...` показывается страница основного приложения (или 404), выполните на **сервере** по шагам.

### 1. Есть ли файл админки в каталоге статики Nginx

У конфига `root /var/www/html/shaolen` — файл должен лежать там же:

```bash
ls -la /var/www/html/shaolen/admin.html
```

Если «No such file or directory» — **админку не копировали.** Скопируйте из репозитория:

```bash
cp /path/to/telegram_goals_bot/webapp/admin.html /var/www/html/shaolen/
```

И при каждом обновлении веб-интерфейса кладите в этот каталог и `admin.html`.

### 2. Что реально отдаёт Nginx по /admin.html

С сервера (подставьте свой домен):

```bash
curl -sI "https://shaolen.duckdns.org/admin.html"
```

- **`HTTP/2 200`** и в конце нет `content-type: text/html` с большим телом — хорошо, дальше смотрите, не подменяет ли отдачу что-то ещё.
- **`HTTP/2 404`** — Nginx не нашёл файл: проверь п.1 и путь `root` в конфиге.
- **`HTTP/2 200`** и в ответе приходят первые строки страницы — посмотрите, начинается ли вывод с `<title>Админ` или с разметки главного приложения. Если с главного — обрабатывается не тот `location` (см. п.4).

Просмотр первых байт тела:

```bash
curl -s "https://shaolen.duckdns.org/admin.html" | head -5
```

Если видите тег `<title>Админ` или блок с «Админ-панель» — отдаётся админка; если там заголовок главного приложения — отдаётся `index.html`.

### 3. Логи Nginx

В них видно, по какому пути пришёл запрос и какой код вернулся.

**Access log** (каждый запрос):

```bash
sudo tail -50 /var/log/nginx/access.log
```

Или только по вашему виртуальному хосту, если настроен отдельный лог:

```bash
sudo tail -50 /var/log/nginx/shaolen-access.log
```

Смотрите строки с `/admin` или `/admin.html` — во второй колонке код ответа (200, 302, 404).

**Error log** (ошибки разбора конфига и «файл не найден»):

```bash
sudo tail -30 /var/log/nginx/error.log
```

Полезно держать их в фоне при проверке в браузере:

```bash
sudo tail -f /var/log/nginx/access.log
# в другом терминале откройте в браузере /admin.html?token=...
```

### 4. Действующий конфиг Nginx (локации для админки)

Убедиться, что в активном конфиге есть обработка админки **до** общего `location /`:

```bash
sudo nginx -T 2>/dev/null | grep -A3 "location.*admin"
```

Должно быть что-то вроде:

```text
location = /admin { ... }
location = /admin.html { ... }
```

Если этих блоков нет — в работу взят не тот конфиг или правки не применены. Проверь файл, из которого собирается конфиг (например `sites-enabled/shaolen`), добавь туда блоки из `nginx/shaolen.conf.example` и перезагрузи конфиг:

```bash
sudo nginx -t && sudo systemctl reload nginx
```

### 5. Кэш браузера и 302 на /admin

Если заходите по `https://домен/admin?token=...`, Nginx делает 302 на `/admin.html?token=...`. Если браузер когда-то закэшировал ответ по `/admin.html`, он может показывать старую страницу. Откройте админку в режиме инкогнито или с очисткой кэша по этому сайту.

---

Итого: чаще всего «открывается главная вместо админки» из‑за того, что **admin.html нет в `root`** Nginx или **в активном конфиге нет/не применены** блоки `location = /admin` и `location = /admin.html`. Проверка по п.1 и п.4 обычно сразу это показывает.

## Упрощенная версия webapp_server.py (только API):

Если хочешь, можно убрать из `webapp_server.py` всё, что связано со статикой, оставив только API endpoints. Но текущая версия тоже работает - просто статика будет игнорироваться, если её отдает Nginx.
